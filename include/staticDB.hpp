#ifndef STATICDB_H
#define STATICDB_H


class staticDB
{
    public:
        staticDB();
        virtual ~staticDB();
        staticDB(const staticDB& other);
        staticDB& operator=(const staticDB& other);

    protected:

    private:
};

#endif // STATICDB_H
